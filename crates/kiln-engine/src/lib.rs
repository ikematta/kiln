#![deny(unsafe_code)]
//! `kiln-engine`: continuous batching loop, paged KV cache, radix prefix
//! cache, sampler, speculative decoding, and SSD cold tier (SPEC §6).
//! Implemented from Phase 4 onward; stub until then.
