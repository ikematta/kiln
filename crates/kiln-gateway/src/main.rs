#![deny(unsafe_code)]
//! `kiln-gateway`: HTTP front end, API adapters, router, and worker
//! supervisor (SPEC §8). Implemented from Phase 2 onward; stub until then.

fn main() {
    eprintln!("kiln-gateway is not implemented yet (Phase 2, see docs/SPEC.md §12)");
    std::process::exit(1);
}
