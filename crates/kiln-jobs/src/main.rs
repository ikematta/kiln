#![deny(unsafe_code)]
//! `kiln-jobs`: download/quantize job runner (SPEC §9.1). Implemented in
//! Phase 10; stub until then.

fn main() {
    eprintln!("kiln-jobs is not implemented yet (Phase 10, see docs/SPEC.md §12)");
    std::process::exit(1);
}
